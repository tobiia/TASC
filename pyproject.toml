[project]
name = "term_ex"
dynamic = ["version"]
description = "An experimentation in terminology extraction and ontology creation"
authors = [{ name = "Tobi Adesanya", email = "tobijadesanya@gmail.com" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
test_term = "term_extraction.main:main"
test_cand = "term_extraction.main:candidate_test"
